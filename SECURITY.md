# Security

Do not commit API keys, OpenClaw tokens, cloud credentials, SSH keys, server IP screenshots or `.env` files.

The public configuration file is `config/openclaw.example.json`; all credential fields are placeholders. Create a local `.env` or private OpenClaw config for deployment.
